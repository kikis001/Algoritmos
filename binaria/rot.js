const nums = [0,1,2,3,4,5,6,7];
const target = 4;
const indice = 3;

function rotated(nums, indice) {
  while(1<=nums.length) {
    g = 0
    t_i = indice + 1
    t = nums[t_i]
    nums[g]
  }
}

function findTarget(nums, target) {

}